function StoreInfo() {
    return (
        <div
            style={{
                width: 600,
                height: 700,
                background: '#fff',
                borderRadius: 25,
                border: '1px solid black', // 올바른 border 값 설정
            }}
        ></div>
    );
}

export default StoreInfo;
